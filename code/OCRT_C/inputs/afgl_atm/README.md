# AFGL/RFM atmosphere profiles

Runtime profiles are selected by `--atm-profile`. File headers document columns,
source lineage and conversion details. `_pre_RFM_backup/` is retained only for audit.
`afgl_userdef.dat` is the fixed-path user-defined template.
